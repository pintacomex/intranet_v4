<div class="authform">
  <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :role => 'form'}) do |f| %>
    <h3>¿Olvidó su contraseña?</h3>
    <p>Enviaremos las instrucciones para resetear la contraseña</p>
    <%= devise_error_messages! %>
    <div class="form-group">
      <%= f.label :email %>
      <%= f.email_field :email, :autofocus => true, class: 'form-control' %>
    </div>
     <%= f.submit 'Reset Password', :class => 'button right' %>
  <% end %>
</div>
<div class="col-md-4 col-sm-4 col-md-offset-4 col-sm-offset-4 well">
    <h3><center>Nota</center></h3>
    <p>Si usted no tiene un email dado de alta con nosotros y no recuerda su password, favor de <a href="/reporte_de_problemas/new/">Reportarlo</a>.</p>
</div>
