<%= simple_form_for(@webhook, url: controller.action_name == 'new' ? notificaciones_webhooks_path : notificaciones_webhook_path(@webhook)) do |f| %>
  <%#= f.error_notification %>

  <div class="form-inputs text-right">
    <%= f.input :name %>
    <%= f.input :value %>
  </div>

  <div class="form-actions text-center">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
