<!-- <div class="container">
	<h1 class="text-center">Mis Tickets</h1>
	<% @my_tickets.each do |ticket| %>
		<div class="card col-md-3 text-right">
			<h5 class="text-center"><%= ticket.title %></h5>
			<hr>
			<h6>Fecha: </h6><p><%= ticket.created_at[0..9] %></p>
			<h6># Ticket: </h6><p><%= ticket.number %></p>
			<% if ticket.close_at != "" %>
				<h6>Cerrado: </h6><p><%= ticket.close_at[0..9] %></p>
			<% end %>
			<h6>Ticket con: </h6><p><%= ticket.group %></p>
			<%= link_to 'Ver Ticket',{:controller => "tickets", :action => "show", :id => ticket.id }, class: 'btn btn-info'  %>
		</div>
	<% end %>
</div>
<style type="text/css">
	.card{
		margin: 25px;
	}
</style>
 -->